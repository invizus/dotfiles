set $mod Mod4
set $term kitty
set $menu wofi --show drun
#font pango:monospace 10
font pango:DejaVu Sans Mono 10
output * scale 1.0

exec_always swayidle -w \
  timeout 300 'swaylock -f -c 000000' \
  timeout 600 'swaymsg "output * dpms off"' \
  resume 'swaymsg "output * dpms on"' \
  before-sleep 'swaylock -f -c 000000'

#exec_always waybar
exec waybar
exec_always wl-paste --watch cliphist store
exec_always kanshi
exec mako

input * {
  xkb_layout gb
  natural_scroll enabled
}

bindsym $mod+Return exec kitty
bindsym $mod+d exec wofi --show drun
bindsym $mod+shift+q kill
bindsym $mod+shift+r reload
bindsym $mod+shift+e exit

bindsym $mod+Left focus left
bindsym $mod+Right focus right
bindsym $mod+Up focus up
bindsym $mod+Down focus down

bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
# Alt+Tab to go to next workspace
bindsym Mod1+Tab workspace next_on_output
bindsym $mod+L exec wlogout
#output * bg /usr/share/backgrounds/sway/Sway_Wallpaper_Blue_1920x1080.png fill

default_border pixel 1
client.focused          #000000 #000000 #ffffff #000000
client.unfocused        #000000 #000000 #888888 #000000
client.urgent           #ff0000 #ff0000 #ffffff #ff0000
client.focused_inactive #000000 #000000 #bbbbbb #000000

gaps inner 10
gaps outer 5

# Send 'lite' to hidden workspace
bindsym $mod+n move container to scratchpad
bindsym $mod+x scratchpad show
bindsym $mod+f fullscreen toggle

# screenshots
#bindsym Print exec grim ~/Pictures/screenshot-$(date +%F-%H%M%S).png
bindsym Print exec grim -g "$(slurp)" ~/Pictures/screenshot-$(date +%F-%H%M%S).png

# Thinkpad Brightness
bindsym XF86MonBrightnessUp exec brightnessctl set +10%
bindsym XF86MonBrightnessDown exec brightnessctl set 10%-

# Thinkpad Volume
bindsym XF86AudioRaiseVolume exec pamixer -i 5
bindsym XF86AudioLowerVolume exec pamixer -d 5
bindsym XF86AudioMute exec pamixer -t

